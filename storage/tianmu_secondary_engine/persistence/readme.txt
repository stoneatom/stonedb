1: Data in rapid should be persistenced to disk periodly, otherwise, we will perform reload operation when the instance crashed. If it has ability to persist to data to disk. If it has.
We will load from disk into rapid directly after instance crashed. 
