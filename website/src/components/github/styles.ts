import styled from 'styled-components';
import { Space } from 'antd';

const GithubStyle = styled(Space)`
    margin: 0 30px;
    .link{
        height: auto;
        line-height: auto;
        border: none;
        padding: 0;
    }
`

export default GithubStyle;